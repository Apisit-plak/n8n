# คู่มือการใช้งาน Formatters - ฉบับสรุป

## 📦 ไฟล์ที่สร้างให้:

| ไฟล์ | ใช้สำหรับ Action | ใช้ใน Node |
|------|------------------|-----------|
| `FORMATTER_TOP_CENTER.js` | top_center | Code In JavaScript (หลัง SQL - Top Center) ✅ ทำแล้ว |
| `FORMATTER_TOP_ITEMS.js` | top_items | Code In JavaScript (หลัง SQL - Top Items) |
| `FORMATTER_MONTHLY_SALES.js` | monthly_sales | Code In JavaScript (หลัง SQL - Monthly Sales) |
| `FORMATTER_CUSTOMER_INVOICES.js` | customer_invoices | Code In JavaScript (หลัง SQL - Customer Invoices) |
| `FORMATTER_INVOICE_DETAIL.js` | invoice_detail | Code In JavaScript (หลัง SQL - Invoice Detail) |
| `FORMATTER_PRODUCT_GROUP.js` | product_group_sales | Code In JavaScript (หลัง SQL - Product Group) |

---

## 🚀 วิธีใช้งาน (แต่ละ Action):

### 1️⃣ Top Items

**ขั้นตอน:**
1. เปิด **Execute SQL Query - Top Items** node
2. **Settings tab** → เปิด ☑️ **Continue on Fail**
3. **Parameters tab** → แก้ SQL บรรทัดสุดท้ายเป็น:
   ```sql
   LIMIT {{ $json.limit || 10 }}
   ```
4. เพิ่ม **Code In JavaScript** node หลัง SQL node
5. Copy โค้ดทั้งหมดจาก `FORMATTER_TOP_ITEMS.js` ไปวาง
6. เชื่อมไปที่ **Respond to Webhook**

---

### 2️⃣ Monthly Sales

**ขั้นตอน:**
1. เปิด **Execute SQL Query - Monthly Sales** node
2. **Settings tab** → เปิด ☑️ **Continue on Fail**
3. เพิ่ม **Code In JavaScript** node หลัง SQL node
4. Copy โค้ดทั้งหมดจาก `FORMATTER_MONTHLY_SALES.js` ไปวาง
5. เชื่อมไปที่ **Respond to Webhook**

---

### 3️⃣ Customer Invoices

**ขั้นตอน:**
1. เปิด **Execute SQL Query - Customer Invoices** node
2. **Settings tab** → เปิด ☑️ **Continue on Fail**
3. เพิ่ม **Code In JavaScript** node หลัง SQL node (ถ้ายังไม่มี)
4. Copy โค้ดทั้งหมดจาก `FORMATTER_CUSTOMER_INVOICES.js` ไปวาง
5. เชื่อมไปที่ **Respond to Webhook**

---

### 4️⃣ Invoice Detail

**ขั้นตอน:**
1. เปิด **Execute SQL Query - Invoice Detail** node
2. **Settings tab** → เปิด ☑️ **Continue on Fail**
3. เพิ่ม **Code In JavaScript** node หลัง SQL node (ถ้ายังไม่มี)
4. Copy โค้ดทั้งหมดจาก `FORMATTER_INVOICE_DETAIL.js` ไปวาง
5. เชื่อมไปที่ **Respond to Webhook**

---

### 5️⃣ Product Group Sales

**ขั้นตอน:**
1. เปิด **Execute SQL Query - Product Group** node (ถ้ามี)
2. **Settings tab** → เปิด ☑️ **Continue on Fail**
3. เพิ่ม **Code In JavaScript** node หลัง SQL node
4. Copy โค้ดทั้งหมดจาก `FORMATTER_PRODUCT_GROUP.js` ไปวาง
5. เชื่อมไปที่ **Respond to Webhook**

---

## ✨ ฟีเจอร์ใน Formatters:

### ✅ **Smart No-Data Handler**
- ตรวจสอบ 3 กรณี: ไม่มีข้อมูล, object ว่าง, ไม่มี key สำคัญ
- แจ้งเตือนชัดเจนพร้อมสาเหตุที่เป็นไปได้
- แนะนำคำถามทางเลือก

### ✅ **Beautiful Formatting**
- ใช้ emoji ทำให้อ่านง่าย
- จัดรูปแบบตัวเลขเป็น Thai locale
- แสดงข้อมูลที่สำคัญก่อน

### ✅ **Contextual Suggestions**
- แนะนำคำถามที่เกี่ยวข้องกับข้อมูลที่แสดง
- Dynamic - ใช้ข้อมูลจริงในคำแนะนำ (เช่น ชื่อศูนย์, ชื่อสินค้า)
- ครอบคลุม 3-5 คำถามถัดไป

---

## 📊 ตัวอย่างผลลัพธ์:

### Top Items (มีข้อมูล):
```
🏆 สินค้าขายดีที่สุด (TOP 10)

📦 1. A-001 - สินค้า A
   💰 ยอดขาย: 2,500,000.00 บาท
   📊 จำนวน: 500 ชิ้น | 125 Invoice
   🏢 ขายได้ 8 ศูนย์: ศูนย์กรุงเทพ, ศูนย์เชียงใหม่, ...

━━━━━━━━━━━━━━━━━━━━━━━━
💡 ต้องการวิเคราะห์เพิ่มเติม?
• "สินค้า A-001 ขายได้ศูนย์ไหนบ้าง" - ดูรายละเอียด
• "สินค้าขายดี 1 ปี" - ดูแนวโน้มระยะยาว
• "product แยกตามกลุ่ม" - ดูตามหมวดหมู่
...
```

### Top Items (ไม่มีข้อมูล):
```
ขออภัยครับ ไม่พบข้อมูลในช่วงเวลาที่ระบุ 😔

🔍 สาเหตุที่เป็นไปได้:
  • ช่วงเวลาที่เลือกอาจยังไม่มีข้อมูล
  • ไม่มีสินค้าขายในช่วงเวลานี้

💡 ลองเปลี่ยนเป็น:
  • "สินค้าขายดีที่สุดปีที่แล้ว" - ดูปีที่แล้ว
  • "TOP 10 สินค้า 6 เดือน" - ดู 6 เดือน
...
```

---

## 🎯 Workflow Structure (ตัวอย่าง):

```
Webhook
  ↓
Code (Parser) - n8n_code_parser.js
  ↓
Switch - Route by Action
  ├─ top_center
  │   ↓
  │   Execute SQL Query (Top Center)
  │   ↓
  │   Code (FORMATTER_TOP_CENTER.js) ✅
  │   ↓
  │   Respond to Webhook
  │
  ├─ top_items
  │   ↓
  │   Execute SQL Query (Top Items)
  │   ↓
  │   Code (FORMATTER_TOP_ITEMS.js) ⚠️ ต้องเพิ่ม
  │   ↓
  │   Respond to Webhook
  │
  ├─ monthly_sales
  │   ↓
  │   Execute SQL Query (Monthly Sales)
  │   ↓
  │   Code (FORMATTER_MONTHLY_SALES.js) ⚠️ ต้องเพิ่ม
  │   ↓
  │   Respond to Webhook
  │
  └─ ... (actions อื่นๆ)
```

---

## ⚙️ Settings ที่ต้องตั้ง:

### ในทุก Execute SQL Query node:
1. คลิกแท็บ **Settings**
2. เปิด ☑️ **Continue on Fail**
3. (Optional) เปิด ☑️ **Always Output Data**

### ในทุก SQL ที่มี TOP/LIMIT:
```sql
-- เปลี่ยนจาก
LIMIT 10

-- เป็น
LIMIT {{ $json.limit || 10 }}
```

---

## 🔍 Troubleshooting:

### ปัญหา: ไม่มีข้อมูลออกมา
**วิธีแก้:**
1. ตรวจสอบว่าเปิด **Continue on Fail** แล้วหรือยัง
2. ตรวจสอบว่า SQL มี WHERE clause ถูกต้อง
3. ตรวจสอบว่า database มีข้อมูลในช่วงเวลานั้นจริง

### ปัญหา: TOP 5 แต่ออกมา 10
**วิธีแก้:**
1. เช็คว่า SQL ใช้ `LIMIT {{ $json.limit || 10 }}` แล้วหรือยัง
2. เช็คว่า parser จับ limit ได้หรือไม่ (ดูที่ WHERE Builder output)

### ปัญหา: Contextual suggestions ไม่ออก
**วิธีแก้:**
1. เช็คว่า Code node มี formatter code แล้วหรือยัง
2. เช็คว่า Respond to Webhook ใช้ `{{ $json.reply }}` แล้วหรือยัง

---

## 📋 Checklist:

### ✅ Parser:
- [x] แก้ unknown message ให้มี contextual suggestions
- [x] แก้ missing name message ให้มี suggestions

### ⚠️ Formatters (ต้องทำต่อ):
- [x] Top Center - ทำแล้ว
- [ ] Top Items - ใช้ `FORMATTER_TOP_ITEMS.js`
- [ ] Monthly Sales - ใช้ `FORMATTER_MONTHLY_SALES.js`
- [ ] Customer Invoices - ใช้ `FORMATTER_CUSTOMER_INVOICES.js`
- [ ] Invoice Detail - ใช้ `FORMATTER_INVOICE_DETAIL.js`
- [ ] Product Group - ใช้ `FORMATTER_PRODUCT_GROUP.js`

### ⚠️ SQL Settings:
- [ ] Top Center - เปิด Continue on Fail
- [ ] Top Items - เปิด Continue on Fail
- [ ] Monthly Sales - เปิด Continue on Fail
- [ ] Customer Invoices - เปิด Continue on Fail
- [ ] Invoice Detail - เปิด Continue on Fail
- [ ] Product Group - เปิด Continue on Fail

### ⚠️ SQL Dynamic LIMIT:
- [ ] Top Center - ใช้ `{{ $json.limit || 10 }}`
- [ ] Top Items - ใช้ `{{ $json.limit || 10 }}`
- [ ] Product Group - ใช้ `{{ $json.limit || 10 }}`

---

## 🎉 เสร็จสิ้น!

เมื่อทำครบทุก checklist แล้ว:
- ✅ ทุก action มี contextual suggestions
- ✅ ทุก error case มีคำแนะนำ
- ✅ ระบบช่วยผู้ใช้สำรวจข้อมูลต่อเนื่อง
- ✅ UX ดีขึ้นมาก!

---

## 💡 Tips:

1. **ทำทีละ action** - แก้ Top Items ก่อน แล้วทดสอบ ค่อยไปต่อ
2. **ทดสอบทั้งกรณีมีข้อมูลและไม่มีข้อมูล** - เพื่อให้มั่นใจว่า handler ทำงาน
3. **ดู execution log** - ถ้ามีปัญหา ดูว่า node ไหนไม่ทำงาน
4. **Copy-paste ทั้งไฟล์** - อย่าลืม copy ทั้งหมด ไม่ใช่แค่บางส่วน

ขอให้โชคดีครับ! 🚀
