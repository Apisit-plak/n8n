# 🚀 Quick Start Guide (ไม่ใช้ Docker)

## ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
npm install
```

## ขั้นตอนที่ 2: ตั้งค่า n8n และ Webhook

1. ให้คุณรัน n8n แยกต่างหาก (เช่น รันด้วย `npx n8n` หรือวิธีที่คุณใช้อยู่)
2. สร้าง workflow ใหม่ใน n8n
3. เพิ่ม **Webhook** node:
   - Method: `POST`
   - Path: `/webhook/chat` (หรือ path ที่คุณต้องการ)
   - Response Mode: `Last Node`
4. เพิ่ม nodes อื่นๆ ตามต้องการ (เช่น Function, HTTP Request, AI, etc.)
5. เพิ่ม **Respond to Webhook** node หรือส่ง response กลับในรูปแบบ:
   ```json
   {
     "response": "คำตอบของคุณ"
   }
   ```
6. **Activate** workflow (เปิดสวิตช์ที่มุมบนขวา)

## ขั้นตอนที่ 3: ตั้งค่า URL ของ n8n Webhook ใน Chat Server

แก้ไขค่า `N8N_WEBHOOK_URL`:

- เปิดไฟล์ `server.js` แล้วเปลี่ยนให้ชี้ไปที่ webhook ของคุณ เช่น:
  ```js
  const N8N_WEBHOOK_URL = 'http://localhost:5678/webhook/chat';
  ```
- หรือกำหนดผ่าน environment variable:
  ```bash
  set N8N_WEBHOOK_URL=http://localhost:5678/webhook/chat
  ```

## ขั้นตอนที่ 4: รัน Chat Server

```bash
npm start
```

หรือสำหรับโหมดพัฒนา:

```bash
npm run dev
```

จากนั้นเปิดเบราว์เซอร์ไปที่:

- Chat Interface: `http://localhost:3000`

## ขั้นตอนที่ 5: ทดสอบการทำงาน

1. เปิดหน้าเว็บ Chat Interface
2. พิมพ์ข้อความแล้วกดส่ง
3. ตรวจสอบว่าได้รับ response จาก n8n

## การแก้ไขปัญหาแบบเร็ว ๆ

- ถ้าไม่ขึ้น response:
  - เช็คว่า n8n ทำงานอยู่และ workflow ถูก activate แล้ว
  - เช็คว่า `N8N_WEBHOOK_URL` ถูกต้อง
  - ดู logs ใน terminal ที่รัน `server.js`
- ถ้าเชื่อมต่อไม่ได้:
  - ตรวจสอบว่า port 3000 (chat) และ port 5678 (n8n) ไม่ชนกับโปรแกรมอื่น
  - ลองเปิด URL ของ webhook โดยตรงจาก Postman / browser เพื่อตรวจสอบ